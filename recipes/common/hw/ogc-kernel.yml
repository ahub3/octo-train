modules:
  - type: containerfile
    snippets:
      - COPY --from=ghcr.io/opengamingcollective/kernel-packages-fedora:latest-fc44 /tmp/rpms/ /tmp/rpms/ogc-kernel
      - RUN find /tmp/rpms/ogc-kernel
      #- RUN dnf install -y /tmp/rpms/ublue-os/ublue-os-nvidia*.rpm
      #- RUN sed -i '0,/enabled=0/{s/enabled=0/enabled=1\npriority=90/}' /etc/yum.repos.d/negativo17-fedora-nvidia.repo   
      #- RUN dnf install -y /tmp/rpms/kmods/kmod-nvidia*.rpm libnvidia-fbc libva-nvidia-driver nvidia-driver nvidia-driver-cuda nvidia-driver-cuda-libs nvidia-modprobe nvidia-persistenced nvidia-settings
      #- RUN dnf install -y  /tmp/rpms/nvidia/nvidia-driver-libs-*.i686.rpm
      #- RUN sed -i '0,/enabled=0/{s/enabled=0/enabled=1\npriority=90/}' /etc/yum.repos.d/nvidia-container-toolkit.repo   
      #- RUN dnf install -y nvidia-container-toolkit nvidia-container-toolkit-base libnvidia-container-tools libnvidia-container1
